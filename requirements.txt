# Runtime dependencies for Streamlit Community Cloud deployment.
# Mirrors [project] dependencies in pyproject.toml — keep the two in sync.
# Local development uses pyproject.toml: uv pip install -e ".[dev]"

numpy>=1.24
scipy>=1.10
matplotlib>=3.7
Pillow>=10.0
streamlit>=1.36
plotly>=5.18
