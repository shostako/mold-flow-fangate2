# 2026-09

## 2026-09-14 リポ立ち上げと形状仕様の読み取り

- fangate v0.7.3 を起点に別リポとして新設（scaffold＋選択コピー方式、fangate 立ち上げ時と同じ）
- 図面 PDF（1:1 スケール、rotation 90）を PyMuPDF でベクタ抽出して実寸で読んだ → `docs/spec.md`
  - 表示座標との対応: display_x = 1684 − PDF y、display_y = PDF x（mediabox は縦 1191 × 1684）
  - ランナのフランクは R12 円に接線ではなく交差（交点 ±11.0 / 深さ 34.6）。角度 14° が最もよく合う（深さ 20 の線幅 139.5 も一致）
  - 断面図から肉厚: エッジから 1 mm は t1.0、19 mm で 1.0→2.5、以降 2.5 均一。井戸もコールドスラッグも無い
  - ゲートは軸上の φ3（固定側からの破線）→ ホットランナーのバルブゲートと読み、射出点 = φ3 ディスク
- ユーザー決定: public・顧客名は出さない／変種は肉盗みだけ残す／Streamlit Cloud に配備
- `core/` 11 モジュール＋`data/materials.json` をコピー（`fan_gate.py` は除外）、builder 非依存のテスト 12 ファイルをコピー
- v0.1.0

## 2026-09-14 PR #1（core 移植）と builder

- PR #1: CI green、Codex 2 件（Cross-WLF 特異点 P1、エアトラップ外縁除外 P2）はどちらも sim/fangate 由来なので Issue #2 / #3 に切り出し
- `gh` は snap 版なので `--body-file` で /tmp 配下を読めない（`open ...: no such file or directory`）。`--body "$(cat ...)"` で渡す。
  `gh repo create --push` も内部の git が `remote-https` を見つけられず失敗するので、リモートを SSH に切り替えて自分で push
- builder `core/fan_runner.py`（v0.2.0）。既定値で製品体積 209.008 cm³（解析値 208.771、+0.11%、302.26 × 220.6 がセルに揃わないぶん）
  - 幅の検算: 深さ 0.5 → 296、20.5 → 136（図面の実線 139.5 と整合）、34.5 → 23（交点）、41.5 → 7（円の底）
  - x 方向の格子ずれを発見: 軸 156.13 がセル中心に乗らず、ランナのラスタ重心が 156.00 に寄る。`grid_shift_x_mm` を足して
    軸をセル境界（製品が偶数列）／セル中心（奇数列）に揃え、鏡映対称テストで固定
  - `test_compression_stroke` の段差保存テストは圧縮部が t4 均一なので builder では段差が作れない → 圧縮マスク内の下半分を
    0.35 に書き換える `_stepped_geometry()` で solver 側の性質だけを見る
- PR #4（builder）Codex P2 × 2、両方直した: 粗メッシュで三角形と円が分断 → ラスタ後の連結チェック（`ndimage.label`）、
  丸端が格子外 → `runner_end_d ≤ runner_w`。validate のパラメトリックテストは順番に注意（`runner_w=20` だけだと先に apex 条件で落ちる）
- ユーザーが `claude setup-token` → `CLAUDE_CODE_OAUTH_TOKEN` を新リポに登録（03:50Z）
- @claude レビュー（PR #4、静的読解のみ）: `_restricted_to` の `product_mask` 欠落を指摘 → 引き継ぎ追加＋テスト。
  対称性テストに cell のパラメトライズ追加。(3) 丸端の円内も深さだけの肉厚則は仕様として明記済みで矛盾なしと判定

## 2026-09-14 Streamlit UI（v0.3.0）

- fangate の `app.py` を Python スクリプトで機械的に変換（import・タイトル・説明文・サイドバー関数・プレビュー注記の置換、
  置換ごとに出現回数を assert）。形状固有は `_fan_runner_sidebar()` に集約、残りは無変更
- プレビューに圧縮部の赤枠（`compression_mask` の contour）。この金型は圧縮部が製品より小さいので明示が要る
- UI テストの罠: fangate 由来の「幅を既定値 315 に戻す」は 302.26 に、額縁の重なりエラーは plate_h 220.6 では frame_w 100 上限で
  作れないので plate_h=30 で起こす、肉盗みの幅上限テストはランナ幅 60 だと apex が円に届かないのでフランク 30° に立ててから幅 80
- ローカル起動＋playwright で既定値の一周を目視
- PR #5: Codex 👍（指摘なし）。@claude (1)(2) 問題なし、(3)「contour(extent, origin=lower) は imshow と半セルずれる」は matplotlib 3.11.2 の
  ソース（`_initialize_x_y` は origin 指定ありでセル中心配置、linspace 外周配置は origin=None のときだけ）と 10×10 マスクの実測
  （等値線が 3.0 / 7.0 のセル境界に乗る）で否定、変更なし。マージして v0.3.0 が main
- 残件: Streamlit Community Cloud への登録（ユーザーがブラウザで）、URL を README / CLAUDE.md に反映。claude.yml の allowedTools
  （action が pytest を実行できず静的読解のみ）は別 PR
